[project]
name = "polars-dataset"
version = "0.1.0"
description = "Some compositions around Polars DataFrames for dealing with experimental datasets"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
